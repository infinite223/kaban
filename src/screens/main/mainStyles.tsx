import { StyleSheet } from "react-native";

export const mainStyles = StyleSheet.create({
    container: {
      flex: .5,
      backgroundColor: '#af1',
      alignItems: 'center',
      justifyContent: 'center',
    },
});